#include <iostream>
#include <iterator>
#include <list>
#include <vector>
#include "p4.h"
using namespace std;


/* int main() {
    vector<int> cnt {1, 3, 4, 1, 3, 2, 3, 4, 6, 5};
    auto res1 = delete_duplicated(cnt);
    for (const auto& item : res1)
        cout << item << " ";
    cout << endl;

    list<int> cnt2 {2, 2, 4, 1, 5, 2, 3, 4, 5, 5};
    auto res2 = delete_duplicated(cnt2);
    for (const auto& item : res2)
        cout << item << " ";
}  */