#include <iostream>
#include <vector>
#include <iterator>
#include <list>
#include "p7.h"
using namespace std;

/* int main() {
    // vector<int> cnt{istream_iterator<int>(cin), istream_iterator<int>()};
    vector<int> cnt = {1, 2, 3, 4, 5, 6};
    auto res =rotate_range(cnt, 2);
    for (const auto& item : res)
        cout << item << " ";

    cout << endl;
    vector<int> cnt2 = {10, 20, 30, 40, 50};
    // vector<int> cnt{istream_iterator<int>(cin), istream_iterator<int>()};
    auto res2 =rotate_range(cnt2, -3);
    for (const auto& item : res2)
        cout << item << " ";

    cout << endl;
    list<int> cnt3 = {1, 2, 3, 4, 5, 6};
    // list<int> cnt{istream_iterator<int>(cin), istream_iterator<int>()};
    auto res3 =rotate_range(cnt3, 2);
    for (const auto& item : res3)
        cout << item << " ";

    cout << endl;
    list<int> cnt4 = {10, 20, 30, 40, 50};
    // list<int> cnt{istream_iterator<int>(cin), istream_iterator<int>()};
    auto res4 =rotate_range(cnt4, -2);
    for (const auto& item : res4)
        cout << item << " ";
}  */