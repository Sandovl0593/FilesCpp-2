#include <iostream>
#include <iterator>
#include <list>
#include <deque>
#include "p3.h"
using namespace std;


/* int main() {
    list<int> cnt {3, 5, 2, 1, 2, 5, 4, 3, 2, 5, 1};
    // list<int> cnt {istream_iterator<int>(cin), istream_iterator<int>()};
    auto res2 = delete_range(cnt, { 1, 2 });
    for (const auto& item : res2)
        cout << item << " ";
    cout << endl;
    list<int> cnt2 {3, 5, 2, 1, 2, 5, 4, 3, 2, 5, 1};
    // deque<int> cnt2{istream_iterator<int>(cin), istream_iterator<int>()};
    auto res3 = delete_range(cnt2, 2);
    for (const auto& item : res3)
        cout << item << " ";
} */