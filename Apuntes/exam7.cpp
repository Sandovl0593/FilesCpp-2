#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

// EJERCICIO 1
template <class T>
void insertion_sort() {

}

int main() {
    chrono::time_point<chrono::system_clock> inicio, fin;
    vector<float> red(10)
    generate(red.begin(), red.end(), [] () { return static_cast<float>((rand()% 1)/100)});

    chrono::time_point<chrono::sy
}